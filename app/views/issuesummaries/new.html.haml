%h1 New Issue Summary
%br

= form_for(@issue_sum, url: create_issuesummaries_path) do |f|
  .row
    .col-md-2
      = f.label :name
      = "*"
    .col-md-10
      = f.text_field :name
  %br/
  .row
    .col-md-6
      .actions
        = f.submit "Create", class: 'btn btn-primary'
        = f.button "Reset", type: :reset, class: "btn btn-primary"
        = link_to "Back", issuesummaries_path , class: 'btn btn-primary button', id: 'back-button'