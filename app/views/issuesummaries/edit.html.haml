%h1 Issue Summary Edit
%br

= form_for(@issue_sum, url: {action: "update"}) do |f|
  .row
    .col-md-2
      = f.label :name
    .col-md-10
      = f.text_field :name
  .row
    .col-md-6
      .actions
        %br/
        = f.submit "Update", class: 'btn btn-primary'
        = link_to "Back", issuesummaries_path , class: 'btn btn-primary button', id: 'back-button'