%h1 Deleted Departments
%br/

%table#myTable.table.table-striped.table-bordered
  %thead
    %tr
      %th Department Id
      %th Name
      %th Deleted_At
      %th Action

  %tbody
    - @departments.each do |department|
      %tr
        %td
          = department.id
        %td
          = department.name
        %td
          = department.deleted_at.in_time_zone("Asia/Kolkata").strftime("%d/%m/%Y")
        %td
          = link_to 'Restore', restore_departments_path(resource_id: department), class: 'btn btn-primary button'