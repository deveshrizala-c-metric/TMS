%h1 Edit Department
%br

= form_for @department, url: {action: "update"}, html: { class: "clientside_validation" } do |f|
  .row
    .form-group
      .col-md-2
        = f.label :name
        = "*"
      .col-md-10
        = f.text_field :name, class: "form-control require_validation length_validation", maxlength: "20"
  .row
    .col-md-6
      .actions
        %br/
        = f.submit "Update", class: 'btn btn-primary'
        = link_to "Back", departments_path , class: 'btn btn-primary button', id: 'back-button'
