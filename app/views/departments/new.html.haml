%h1 New Department
%br

= form_for(@department, url: {action: "create"}) do |f|
  .row
    .col-md-2
      = f.label :name
    .col-md-10
      = f.text_field :name
      = "*"
  .row
    .col-md-2
      .actions
        %br/
        = f.submit "Create", class: 'btn btn-primary'