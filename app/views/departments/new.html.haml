%h1 New Department
%br

= form_for(@department, url: create_departments_path) do |f|
  .row
    .col-md-2
      = f.label :name
      = "*"
    .col-md-10
      = f.text_field :name
  %br/
  .row
    .col-md-6
      .actions
        = f.submit "Create", class: 'btn btn-primary'
        = f.button "Reset", type: :reset, class: "btn btn-primary"
        = link_to "Back", departments_path , class: 'btn btn-primary button', id: 'back-button'