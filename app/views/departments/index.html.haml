%h1 Departments
%br/

%table#myTable.table.table-striped.table-bordered
  %thead
    %tr
      %th Department Id
      %th Name
      %th Action

  %tbody
    - @departments.each do |department|
      %tr
        %td
          = department.id
        %td
          = department.name
        %td
          = link_to department do
            %i.glyphicon.glyphicon-eye-open
          = link_to edit_department_path(department) do
            %i.glyphicon.glyphicon-pencil
          = link_to department, method: :delete, data: { confirm: 'Are you sure you want to delete Department? Once you delete department the tickets related department also deleted?' } do
            %i.glyphicon.glyphicon-remove