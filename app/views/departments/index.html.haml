%h1 Departments
%br/

%table#myTable.table.table-striped.table-bordered
  %thead
    %tr
      %th Department Id
      %th Name
      %th Created_At
      %th Action

  %tbody
    - @departments.each do |department|
      %tr
        %td
          = department.id
        %td
          = department.name
        %td
          = department.created_at.in_time_zone("Asia/Kolkata").strftime("%d/%m/%Y")
        %td
          = link_to department, class: "tag-tooltip", :data => {:toggle=>"tooltip"}, 'data-original-title' => "View", 'data-placement' => 'right' do
            %i.glyphicon.glyphicon-eye-open
          = link_to edit_department_path(department), class: "tag-tooltip", :data => {:toggle=>"tooltip"}, 'data-original-title' => "Edit", 'data-placement' => 'right' do
            %i.glyphicon.glyphicon-pencil
          = link_to department, class: "tag-tooltip", :data => {:toggle=>"tooltip"}, 'data-original-title' => "Delete", 'data-placement' => 'right', method: :delete, data: { confirm: ' Are you sure you want to delete this Department? All the tickets related to this department will also be deleted !' } do
            %i.glyphicon.glyphicon-remove