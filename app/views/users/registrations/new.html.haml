%h1 Sign up
%p
  Use the forms below to create or update the information we have on file for your account

= form_for(resource, as: resource_name, url: registration_path(resource_name), :html => { :id => "clientLogin", class: "user-form" }) do |f|

  - if @user.errors.any?
    .alert.alert-error
      - @user.errors.full_messages.each do |message|
        %li= message
        %br
    %br

  .field
    = f.label :fullname
    %br/
    = f.text_field :fullname, autofocus: true, class: "form-control"
  .field
    = f.label :phone
    %br/
    = f.text_field :phone, class: "form-control"
  .field
    = f.label :email
    %br/
    = f.email_field :email, class: "form-control", placeholder: "example@example.com"
  .field
    = f.label :password
    - if @minimum_password_length
      %em
        (#{@minimum_password_length} characters minimum)
    %br/
    = f.password_field :password, autocomplete: "off", class: "form-control"
  .field
    = f.label :password_confirmation
    %br/
    = f.password_field :password_confirmation, autocomplete: "off", class: "form-control"
  %br/
  .actions
    = f.submit "Sign up", class: 'btn btn-primary'
  %p
    = render "users/shared/links"
