%h1 Deleted Issue Summaries
%br/

%table#myTable2.table.table-striped.table-bordered
  %thead
    %tr
      %th Issue Summary Id
      %th Name
      %th Deleted_At
      %th Action

  %tbody
    - @issue_sums.each do |issue_sum|
      %tr
        %td
          = issue_sum.id
        %td
          = issue_sum.name
        %td
          = issue_sum.deleted_at.in_time_zone("Asia/Kolkata").strftime("%d/%m/%Y")
        %td
          = link_to restore_issue_summaries_path(resource_id: issue_sum), class: "tag-tooltip", :data => {:toggle=>"tooltip"}, 'data-original-title' => "Restore", 'data-placement' => 'right' do
            %i.glyphicon.glyphicon-repeat